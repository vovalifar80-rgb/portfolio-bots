services:
  - type: worker
    name: portfolio-demo-bot
    env: python
    plan: free
    pythonVersion: "3.11.8"
    buildCommand: pip install -r requirements.txt
    startCommand: python demo_bot.py
    envVars:
      - key: BOT_TOKEN
        value: твой_токен_здесь
